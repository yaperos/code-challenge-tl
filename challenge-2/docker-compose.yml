version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: yape-challenge-2-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      # Let DB creation be automatic for default db
      POSTGRES_DB: yape_transfer
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
